
# Phase 2 LP vaults. Deployed on-chain BEFORE being announced, so the /lp route 404s
# unless this is exactly "true". Flip it to reveal Phase 2 — nothing else needs changing.
NEXT_PUBLIC_LP_ENABLED=false

# Address Lookup Table for Raydium LP (required — Raydium LP instructions do not fit a
# legacy transaction). Create with keeper/src/scripts/create-lp-alt.ts
NEXT_PUBLIC_LP_ADDRESS_LOOKUP_TABLE=
