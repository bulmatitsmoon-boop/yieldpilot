# Pure-logic checks that mirror the on-chain program's accounting, runnable without a build,
# a validator, or a wallet. verify-settle-recall.mjs mirrors lib.rs's settle_recall and
# asserts the proportional recall accounting (full/partial/gain/loss, no residue drift).
# verify-clear-phantom.mjs mirrors clear_phantom_deployed's cleanup accounting.
# verify-accrue-sol-lst.mjs mirrors accrue_sol_lst's real-yield-booking accounting.
# verify-lifetime-gains.mjs mirrors the lifetime_gains cumulative counter (settle_recall's
# gain branch + accrue_sol_lst's gain branch increment it; nothing else touches it).
# verify-withdraw-charges-own-recall-loss.mjs mirrors the pending_recall_loss charge-back:
# a withdrawal that forces an early recall pays its OWN exit-fee loss via a reduced
# payout (reverting if the loss exceeds the withdrawal), instead of that loss being
# socialized into total_deposits for every depositor.
# Keep each mirror in sync if the Rust changes.
name: Program Logic

on:
  pull_request:
    paths:
      - "tests/verify-settle-recall.mjs"
      - "tests/verify-clear-phantom.mjs"
      - "tests/verify-accrue-sol-lst.mjs"
      - "tests/verify-lifetime-gains.mjs"
      - "tests/verify-withdraw-charges-own-recall-loss.mjs"
      - "programs/yieldpilot/src/lib.rs"
      - ".github/workflows/program-logic.yml"
  push:
    branches: [main]
    paths:
      - "tests/verify-settle-recall.mjs"
      - "tests/verify-clear-phantom.mjs"
      - "tests/verify-accrue-sol-lst.mjs"
      - "tests/verify-lifetime-gains.mjs"
      - "tests/verify-withdraw-charges-own-recall-loss.mjs"
      - "programs/yieldpilot/src/lib.rs"

jobs:
  settle-recall:
    name: settle_recall accounting
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tests/verify-settle-recall.mjs

  clear-phantom:
    name: clear_phantom_deployed accounting
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tests/verify-clear-phantom.mjs

  accrue-sol-lst:
    name: accrue_sol_lst accounting
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tests/verify-accrue-sol-lst.mjs

  lifetime-gains:
    name: lifetime_gains accounting
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tests/verify-lifetime-gains.mjs

  withdraw-charges-own-recall-loss:
    name: withdraw() charges its own forced-recall loss
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: node tests/verify-withdraw-charges-own-recall-loss.mjs
